// ============================================================
// 练习 e08: 注释写法 — view! 宏内的注释语法
//
// 核心知识点:
//   - /* */ 块注释（可跨行）
//   - // 行注释（Leptos 0.9 nightly 支持）
//   - 注释在编译时被忽略，不影响渲染结果
//
// 难度: ⭐ (填空题 — 每行都有 TODO 指引)
// ============================================================

use leptos::prelude::*;

#[component]
fn Exercise() -> impl IntoView {
    // TODO: 在 view! 宏中编写带注释的页面
    view! {
        <div>
            /* 这是 view! 宏内的块注释 */
            // TODO: 添加一个 <p> 元素，显示 "注释不会出现在渲染结果中"
            // 提示: 使用 <p>"文本"</p> 格式
            <p>"注释不会出现在渲染结果中"</p>

            /* 多行注释
               也可以跨行书写
            */
            // TODO: 添加一个 <p> 元素，说明多行注释在编译时被忽略
            // 提示: 文本内容为 "上面的注释在编译时会被忽略"
            <p>"上面的注释在编译时会被忽略"</p>

            // 行注释也支持（Leptos 0.9 nightly）
            // TODO: 添加一个 <p> 元素，说明行注释同样不会渲染
            // 提示: 文本内容为 "行注释同样不会渲染"
            <p>"行注释同样不会渲染"</p>
        </div>
    }
}

fn main() {
    mount_to_body(Exercise);
}

// <details>
// 参考答案（去除注释后的纯净版本）:
//
// use leptos::prelude::*;
//
// #[component]
// fn Exercise() -> impl IntoView {
//     view! {
//         <div>
//             /* 这是 view! 宏内的块注释 */
//             <p>"注释不会出现在渲染结果中"</p>
//
//             /* 多行注释
//                也可以跨行书写
//             */
//             <p>"上面的注释在编译时会被忽略"</p>
//
//             // 行注释也支持（Leptos 0.9 nightly）
//             <p>"行注释同样不会渲染"</p>
//         </div>
//     }
// }
//
// fn main() {
//     mount_to_body(Exercise);
// }
//
// 知识点:
// 1. /* */ 和 // 注释在 view! 宏中均可使用
// 2. 注释只在源码中可见，不会出现在最终渲染的 DOM 中
// 3. /* */ 支持跨行，// 仅支持单行
// </details>
